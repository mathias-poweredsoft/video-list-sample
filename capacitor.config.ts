import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'video-list-sample',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
